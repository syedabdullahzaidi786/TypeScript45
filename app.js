"use strict";
let names = ['Syed Abdullah', 'Raheem', 'Rajab', 'Farjad'];
const message = 'Welcome to Typescript code: ';
for (let i = 0; i < names.length; i++) {
    console.log(message + names[i]);
}
